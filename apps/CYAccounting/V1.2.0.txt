Version: V1.2.0
Date: 2026/09/25

Current source on Phase 1 visual-alignment branch; test artifact, not a formal Release:
- Aligned CYAccounting with the canonical CY Desktop Visual Guide Phase 1 using the Blue Theme.
- Main accounting workspace moves toward the recommended Continuous surface model; result/statistics areas remain carded where they are independent information units.
- Main tabs use a lower-presence header treatment with an accent underline; page chrome and decorative borders are reduced.
- Input page follows a dedicated data-entry density: primary entry fields remain deliberately larger and more legible (11pt, at least 30px high) because entering data is the primary task, while unnecessary section/margin whitespace is reduced.
- Ledger follows a separate high-density review mode: both data rows and the table header are 24px high; query/header/statistics spacing is tightened without reducing the 10pt table text.
- Inputs, buttons and dialogs stop forcing overly compressed single-line control heights; focus styling no longer changes border geometry.
- Ledger uses neutral headers, low-contrast grid, soft selection and a single zebra-striping source.
- Monetary entry fields are right-aligned; state colors and accounting-domain income/expense colors are separated.
- Settings and Import dialogs receive canonical spacing/density treatment while preserving their existing behavior and workflow.
- Existing accounting workflow, Enter/Tab/Esc behavior, database schema, backup/restore, Google Drive, import flow, locking and two-DELETE local-ledger reset behavior remain unchanged.
- ACC Icon Family from V1.1.0 remains unchanged.
- Ledger inline-edit ComboBox narrow-arrow treatment is intentionally unchanged pending user decision because it preserves high-density cell text space.
- 125% / 150% DPI acceptance remains deferred under the canonical guide; this test version does not claim those DPI levels as passed.
- Input page refinement: framed Basic/Income/Expense sections restored; Income uses CYAccountingWeb #E7F3E9 and Expense uses #F8E9E7; legacy polished category-combo arrow treatment restored only on the input page; date controls refined and matched in height; confirmation panel is fixed to the 10-entry use case without a white card fill; ledger data rows restored to the original 23px density while the header stays 24px.
