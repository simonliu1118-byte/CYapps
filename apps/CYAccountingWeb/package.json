{
  "name": "cyaccounting-web",
  "version": "0.4.0",
  "private": true,
  "description": "CYAccounting Web - Cloudflare Workers + D1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply cyaccounting-web-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply cyaccounting-web-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
